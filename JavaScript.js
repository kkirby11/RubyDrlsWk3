// Write a script function that returns a passed string with letters in alphabetical order.
// Example string : 'webmaster'
// Expected Output : 'abeemrstw'

// function alphabet_order(str)
//   {
// return str.split('').sort().join('');
//   }
// console.log(alphabet_order("webmaster"));

//---------------
// Write a script that accepts a string as a parameter and find the longest word within the string.
// Example string : 'Web Development Tutorial'
// Expected Output : 'Development'
// function length_order(str)
//   {
//  return str.split('').sort().join('');
//    }
//  console.log(length_order("webmaster"));
//---------------

// Write a script that checks whether a passed string is palindrome or not. 
// A palindrome is word, phrase, or sequence that reads the same backward as forward, 
// e.g., madam or nurses run. Make sure to take care of spaces and capital letters.


var a = new Array()
a.sort();
a.sort(function(b,a)  {
    return b-a;
    });
a.sort(funtion(b,a) {return b-a}); 
console.log a


